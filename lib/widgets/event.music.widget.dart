import 'package:flutter/material.dart';

class EventMusicWidget extends StatelessWidget {
  const EventMusicWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Column(
      children: <Widget>[
        Text('Musicos'),
      ],
    );
  }
}
