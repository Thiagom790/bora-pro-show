import 'package:flutter/material.dart';

class EventAddressWidget extends StatelessWidget {
  const EventAddressWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Column(
      children: <Widget>[
        Text('Endereço'),
      ],
    );
  }
}
